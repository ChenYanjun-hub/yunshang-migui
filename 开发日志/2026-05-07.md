# 开发日志 · 2026-05-07

> Phase 1 第 1 晚 — Supabase 项目落地 + 用户认证

## 一、本日交付

### 1. Supabase 后端
- 项目地区：Northeast Asia (Tokyo)
- 完成：8 张表 + RLS 策略 + auth 触发器（自动建 profile）
- 表清单：`profiles` `archives` `archive_likes` `products` `checkins` `posts` `post_likes` `comments`
- 角色枚举：`user / admin_product / admin_tech / admin_ops / admin_biz / super_admin`
- Storage bucket：`public-assets`（公开读）
- Auth：邮箱+密码，**关闭邮件确认**（演示便利）

### 2. 前端认证基建
- 装包：`@supabase/supabase-js` `@supabase/ssr`
- `src/lib/supabase/client.ts` — 浏览器端 Supabase client
- `src/lib/supabase/server.ts` — 服务端 Supabase client（带 cookie 桥接）
- `proxy.ts`（**Next 16 重命名自 middleware.ts**）— 会话刷新 + 路由守卫
  - 守卫路径：`/user`、`/admin`
  - 已登录访问 `/auth/*` 自动跳转 `/user`

### 3. 认证页面
- `/auth/login` — 邮箱密码登录；预留"手机登录"Tab（占位文案，UI 已就位）
- `/auth/register` — 昵称 + 邮箱 + 密码注册
- `src/app/auth/actions.ts` — Server Actions：`signInWithPassword` / `signUpWithPassword` / `signOut`

### 4. 用户中心改造
- `src/app/user/page.tsx` 改为 Server Component
- 读取真实 `auth.users` + `profiles` 数据
- 显示真实昵称、邮箱、注册年份、角色徽章
- admin 角色显示 "Admin Panel →" 入口
- 增加"退出登录"按钮（Server Action）

## 二、技术决策记录

### 关于"国内访问"
- 答辩冲刺暂按方案 A：Vercel + Supabase
- 答辩当日如评委用国内网络出问题，本地起服务投屏演示
- 长期运营方案（迁阿里云/腾讯云）排到答辩后

### 关于手机号验证码
- 国内手机号通过 Supabase 走国际短信通道到达率低、需备案
- 当前选邮箱+密码，UI 保留手机号 Tab 占位
- 答辩话术："手机号验证码通道已预留接口，正在对接国内短信服务商"

### Next 16 关键变更（已落地）
- `middleware.ts` → `proxy.ts`，导出函数名 `proxy`
- `cookies()` 必须 `await`
- 已读 `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md`
  和 `04-functions/cookies.md` 确认 API

## 三、文件改动清单
- 新增：`.env.local`、`proxy.ts`
- 新增：`src/lib/supabase/{client,server}.ts`
- 新增：`src/app/auth/{login,register}/page.tsx`、`src/app/auth/actions.ts`
- 修改：`src/app/user/page.tsx`（client → server，接入真实数据）

## 四、验证结果
- `npx tsc --noEmit`：新增代码 0 错误（archive 页历史报错与本次无关）
- 冒烟：`GET /auth/login` → 200；未登录 `GET /user` → 307 redirect 到 login

## 五、加班产出（同晚追加）

### A. 后台骨架（admin shell）
- `src/app/admin/layout.tsx` — Server Component，登录+角色双重守卫；左侧栏按角色过滤菜单
- `src/app/admin/page.tsx` — 总览（已发布史料/在架商品/帖子数/用户数 4 个卡片，从 DB 实时统计）
- `src/app/admin/archives/page.tsx` — 史料列表（只读表格，"新增"按钮明晚做）
- `src/app/admin/products/page.tsx` `community/page.tsx` — 占位
- `src/app/admin/users/page.tsx` — 用户列表（昵称/角色/注册时间）
- 角色 → 菜单映射：
  - `admin_product` / `super_admin` → 史料管理
  - `admin_biz` / `super_admin` → 商品管理
  - `admin_ops` / `super_admin` → 社区审核
  - `super_admin` → 用户管理

### C. 前台 `/archive` 接真实数据
- DB 改造：`alter table archives add column year int, image_height int`
- 灌入 6 条已发布的种子史料（picsum 占位图）
- 重构 `src/app/archive/page.tsx` 为 Server Component，仅负责取数 + 映射
- 新增 `src/app/archive/ArchiveClient.tsx` 承接所有筛选/lightbox 客户端逻辑
- **顺手修复**了原 archive 页 16 处 enum 类型错误（改用字符串字面量联合）

### 验证
- 注册账号 → SQL 升级 super_admin → 登录 → 看到徽章 + Admin Panel 入口 ✅
- 点 Admin Panel → 总览数据正确（已发布史料 6 / 用户 N）✅
- 前台 `/archive` 看到 6 条真实史料，筛选/lightbox 正常 ✅
- `npx tsc --noEmit`：0 错误（连历史报错也清了）

## 六、明天（5月8日）任务

> Phase 1 第 2 晚 — 史料 / 商品 CRUD + 图片上传

- [ ] 史料 CRUD 表单（新增 / 编辑 / 删除 / 状态切换 草稿↔发布↔归档）
- [ ] Storage 图片上传组件（拖拽 + 多图 + 进度提示）
- [ ] 商品 CRUD（结构与史料类似，可复用表单组件）
- [ ] 社区审核简版（列表 + 隐藏帖子）
- [ ] 用户管理：UI 升级/降级角色（替代手写 SQL）

## 六、答辩前明确的待办（提醒未来的我）

- [ ] 5月13日前给评委演示账号录入若干 admin
- [ ] `.env.local` **不要**提交到 git（确认 `.gitignore` 含 `.env*`）
- [ ] `Confirm email` 关闭只是演示态；正式上线必须重新打开
- [ ] `archive/page.tsx` 历史 TS 报错独立修复
